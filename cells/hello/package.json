{
  "name": "@otavia-starter/hello",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run --hot backend/app.ts",
    "build": "bun build backend/app.ts --outdir dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
